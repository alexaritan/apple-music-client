{
	"devDependencies": {
		"eslint": "^8.5.0",
		"husky": "^7.0.4",
		"jest": "^27.4.5",
		"typescript": "^4.5.4"
	},
	"jest": {
		"collectCoverage": true,
		"coverageReporters": [
			"text"
		],
		"testMatch": [
			"<rootDir>/build/**/*.test.js"
		],
		"verbose": true
	},
	"scripts": {
		"build": "rm -rf ./build && tsc -p .",
		"lint": "eslint . --ext ts",
		"test": "jest"
	}
}
